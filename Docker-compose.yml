version: "3.8"
services:
  nginx:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: nginx_service
    restart: always
    environment:
      MYVARIABLE: esto es una variable de prueba
    ports:
      - 8080:80
